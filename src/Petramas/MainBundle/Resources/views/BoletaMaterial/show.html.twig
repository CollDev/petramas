{% extends 'PetramasMainBundle::layout.html.twig' %}

{% block content -%}
<h1>Boleta Material</h1>
<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Neto</th>
            <td>{{ entity.neto }}</td>
        </tr>
    </tbody>
</table>
<a class="btn btn-info pull-right" href="{{ path('boletamaterial') }}">Volver</a>
<a class="btn btn-warning pull-right" href="{{ path('boletamaterial_edit', { 'id': entity.id }) }}">Editar</a>
{{ form(delete_form) }}
{% endblock %}
