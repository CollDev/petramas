{% extends 'PetramasMainBundle::layout.html.twig' %}

{% block content -%}
<h1>RecepcionMaterial</h1>
<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Fechaingreso</th>
            <td>{{ entity.fechaIngreso|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Cantidad</th>
            <td>{{ entity.cantidad }}</td>
        </tr>
    </tbody>
</table>
<a class="btn btn-info pull-right" href="{{ path('recepcionmaterial') }}">Volver</a>
<a class="btn btn-warning pull-right" href="{{ path('recepcionmaterial_edit', { 'id': entity.id }) }}">Editar</a>
{{ form(delete_form) }}
{% endblock %}
