{% extends '::base.html.twig' %}

{% block body -%}
    <h1>LiquidacionMaterial</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Fechaliquidacion</th>
                <td>{{ entity.fechaLiquidacion|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Importe</th>
                <td>{{ entity.importe }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('liquidacionmaterial') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('liquidacionmaterial_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
