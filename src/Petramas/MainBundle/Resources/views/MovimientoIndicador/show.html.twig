{% extends '::base.html.twig' %}

{% block body -%}
    <h1>MovimientoIndicador</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Valor</th>
                <td>{{ entity.valor }}</td>
            </tr>
            <tr>
                <th>Fechamovimiento</th>
                <td>{{ entity.fechaMovimiento|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('movimientoindicador') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('movimientoindicador_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
