{% extends '::base.html.twig' %}

{% block body -%}
    <h1>IncidenciaResolucion</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Fecharesolucion</th>
                <td>{{ entity.fechaResolucion|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Resolucion</th>
                <td>{{ entity.resolucion }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('incidenciaresolucion') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('incidenciaresolucion_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
