{% extends 'PetramasMainBundle::layout.html.twig' %}

{% block content -%}
<h1>IncidenciaResolucion</h1>
<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Fecharesolucion</th>
            <td>{{ entity.fechaResolucion|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Resolucion</th>
            <td>{{ entity.resolucion }}</td>
        </tr>
    </tbody>
</table>
<a class="btn btn-info pull-right" href="{{ path('incidenciaresolucion') }}">Volver</a>
<a class="btn btn-warning pull-right" href="{{ path('incidenciaresolucion_edit', { 'id': entity.id }) }}">Editar</a>
{{ form(delete_form) }}
{% endblock %}
