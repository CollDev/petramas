{% extends '::base.html.twig' %}

{% block body -%}
    <h1>IncidenciaResolucion list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Fecharesolucion</th>
                <th>Resolucion</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('incidenciaresolucion_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{% if entity.fechaResolucion %}{{ entity.fechaResolucion|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.resolucion }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('incidenciaresolucion_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('incidenciaresolucion_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('incidenciaresolucion_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
