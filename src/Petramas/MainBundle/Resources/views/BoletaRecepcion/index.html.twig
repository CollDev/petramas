{% extends 'PetramasMainBundle::layout.html.twig' %}

{% block content -%}
                <h1>
                    Boleta Recepciones
                    <a class="btn btn-primary entity pull-right" href="{{ path('boletarecepcion_new') }}" rel="tooltip" title="Nuevo">
                        <span class="glyphicon glyphicon-file"></span>
                    </a>
                </h1>
                <div class="table-responsive">
                    <table class="table table-bordered table-striped table-condensed">
                        <thead>
                            <tr>
                                <th>Nº</th>
                                <th>Fechaingreso</th>
                                <th>Fechasalida</th>
                                <th>Total</th>
                                <th>Tara</th>
                                <th>Neto</th>
                                <th>Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for entity in entities -%}
                            <tr>
                                <td>{{ loop.index }}</td>
                                <td>{% if entity.fechaIngreso %}{{ entity.fechaIngreso|date('Y-m-d H:i:s') }}{% endif %}</td>
                                <td>{% if entity.fechaSalida %}{{ entity.fechaSalida|date('Y-m-d H:i:s') }}{% endif %}</td>
                                <td>{{ entity.total }}</td>
                                <td>{{ entity.tara }}</td>
                                <td>{{ entity.neto }}</td>
                                <td>
                                    <a class="btn btn-info btn-xs entity" href="{{ path('boletarecepcion_show', { 'id': entity.id }) }}" rel="tooltip" title="Detalle"><span class="glyphicon glyphicon-eye-open"></span></a>
                                    <a class="btn btn-warning btn-xs entity" href="{{ path('boletarecepcion_edit', { 'id': entity.id }) }}" rel="tooltip" title="Editar"><span class="glyphicon glyphicon-pencil"></span></a>
                                    <a class="btn btn-danger btn-xs delete" href="{{ path('boletarecepcion_delete', { 'id': entity.id }) }}" rel="tooltip" title="Eliminar"><span class="glyphicon glyphicon-remove"></span></a>
                                </td>
                            </tr>
                            {% endfor -%}
                        </tbody>
                    </table>
                </div>
{% endblock %}
